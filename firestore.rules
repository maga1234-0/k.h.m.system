
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    /**
     * Core Philosophy:
     * Restricted access for ImaraPMS.
     * Support for invitation-based staff onboarding.
     */

    // --- Helper Functions ---

    function isSignedIn() {
      return request.auth != null;
    }

    // Check if the user is a staff member
    function isStaff() {
      return isSignedIn() && exists(/databases/$(database)/documents/staff/$(request.auth.uid));
    }

    // Check if the user is an admin or manager
    function isAdmin() {
      return isSignedIn() && 
             (request.auth.token.email == 'aubinmaga@gmail.com' ||
              exists(/databases/$(database)/documents/roles_admin/$(request.auth.uid)));
    }

    // --- Rules ---

    /**
     * @description Allow public read of settings to display hotel name/branding.
     */
    match /settings/{settingId} {
      allow read: if true;
      allow write: if isAdmin();
    }

    /**
     * @description Staff management: 
     * - Public list/read allowed for login invitation check.
     * - A user can delete their temporary invitation record during onboarding.
     */
    match /staff/{staffId} {
      allow read, list: if true; 
      allow create, update: if isAdmin() || (isSignedIn() && request.auth.uid == staffId);
      // Allow a user to delete a record if it matches their email (for onboarding cleanup)
      allow delete: if isAdmin() || (isSignedIn() && (resource.data.email == request.auth.token.email || request.auth.uid == staffId));
    }

    /**
     * @description Admin role assignment.
     */
    match /roles_admin/{staffId} {
      allow read: if isSignedIn();
      allow create: if isSignedIn() && (request.auth.token.email == 'aubinmaga@gmail.com' || isAdmin() || request.auth.uid == staffId);
      allow update, delete: if isAdmin();
    }

    /**
     * @description Core hotel data: Accessible to all signed-in staff.
     */
    match /rooms/{roomId} {
      allow read, write: if isSignedIn();
    }

    match /reservations/{resId} {
      allow read, write: if isSignedIn();
    }

    match /clients/{clientId} {
      allow read, write: if isSignedIn();
    }

    match /invoices/{invId} {
      allow read, write: if isSignedIn();
    }

    /**
     * @description Default deny for everything else.
     */
    match /{document=**} {
      allow read, write: if isAdmin();
    }
  }
}
