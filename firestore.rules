
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    /**
     * Core Philosophy:
     * Restricted access for ImaraPMS.
     * Support for invitation-based staff onboarding.
     */

    // --- Helper Functions ---

    function isSignedIn() {
      return request.auth != null;
    }

    // Check if the user is a staff member (without circular logic)
    function isStaff() {
      return isSignedIn() && exists(/databases/$(database)/documents/staff/$(request.auth.uid));
    }

    // Check if the user is an admin or manager
    function isAdmin() {
      return isSignedIn() && 
             (request.auth.token.email == 'aubinmaga@gmail.com' ||
              exists(/databases/$(database)/documents/roles_admin/$(request.auth.uid)));
    }

    // --- Rules ---

    /**
     * @description Allow public read of settings to display hotel name/branding.
     */
    match /settings/{settingId} {
      allow read: if true;
      allow write: if isAdmin();
    }

    /**
     * @description Staff management: 
     * - Anyone can check for an invitation by email (read/list).
     * - A user can create/update their own profile during onboarding.
     */
    match /staff/{staffId} {
      allow read: if true;
      allow list: if true; 
      allow create: if isAdmin() || (isSignedIn() && request.auth.uid == staffId);
      allow update: if isAdmin() || (isSignedIn() && request.auth.uid == staffId);
      allow delete: if isAdmin();
    }

    /**
     * @description Admin role assignment.
     * Allow users to register their own admin record if they are being promoted.
     */
    match /roles_admin/{staffId} {
      allow read: if isSignedIn();
      allow create: if isSignedIn() && (request.auth.token.email == 'aubinmaga@gmail.com' || isAdmin() || request.auth.uid == staffId);
      allow update, delete: if isAdmin();
    }

    /**
     * @description Core hotel data: Accessible to all signed-in staff.
     */
    match /rooms/{roomId} {
      allow read: if isSignedIn();
      allow write: if isSignedIn();
    }

    match /reservations/{resId} {
      allow read: if isSignedIn();
      allow write: if isSignedIn();
    }

    match /clients/{clientId} {
      allow read: if isSignedIn();
      allow write: if isSignedIn();
    }

    match /invoices/{invId} {
      allow read: if isSignedIn();
      allow write: if isSignedIn();
    }

    /**
     * @description Default deny for everything else.
     */
    match /{document=**} {
      allow read, write: if isAdmin();
    }
  }
}
